# LAPTOP SPECIFIC CONFIG — Template
# ----------------------
# Customize the monitor line to match your display.
# Run `hyprctl monitors all` to find your monitor name and resolution.

# 1. Monitor: Auto-detect (safe default)
monitor=,preferred,auto,auto

# 2. Input: Enable Trackpad Gestures
input {
    kb_layout = us
    follow_mouse = 1
    numlock_by_default = true 

    touchpad {
        natural_scroll = true
        tap-to-click = true
        drag_lock = true
        disable_while_typing = true
    }

    accel_profile = adaptive
    sensitivity = 0
}

# 3. Laptop Keybinds
bind = $mainMod, M, exec, ~/.config/hypr/scripts/mirror-hotplug.sh
bindel = ,XF86MonBrightnessUp, exec, ~/.config/swaync/scripts/brightness.sh up
bindel = ,XF86MonBrightnessDown, exec, ~/.config/swaync/scripts/brightness.sh down
